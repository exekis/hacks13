#!/bin/bash

# backend startup script

echo "[backend] starting backend setup..."

cd "$(dirname "$0")/../backend" || exit 1

# TODO: add backend startup commands here
# e.g., source venv/bin/activate
# e.g., uvicorn app.main:app --reload

echo "[backend] backend startup script is a placeholder."
